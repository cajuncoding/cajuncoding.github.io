# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Pin ffi to a version compatible with Ruby 2.6 (used by jekyll/builder:3.8).
# TODO: Upgrade to jekyll/builder:4.x (Ruby 3.x) and remove this constraint.
# Ruby 2.6 is EOL since March 2022; ffi >= 1.17 requires Ruby >= 3.0.
gem "ffi", "< 1.17"

